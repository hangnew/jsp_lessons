function add(x, y) {
	return x + " + " + y + " = " + (x + y);
}

function sub(x, y) {
	return x + " - " + y + " = " + (x - y);
}

function mul(x, y) {
	return x + " * " + y + " = " + (x * y);
}

function div(x, y) {
	return x + " / " + y + " = " + (x / y);
}